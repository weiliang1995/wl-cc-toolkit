# PowerShell 5.1 的 here-string（@'...'@）在 LF 换行下解析失败，
# 会把内嵌的 C# 当成 PowerShell 代码。ps1 必须保持 CRLF。
*.ps1 text eol=crlf

# Shell scripts must keep LF: core.autocrlf=true would otherwise check them
# out with CRLF, and bash fails with "/bin/bash^M: bad interpreter".
*.sh text eol=lf
